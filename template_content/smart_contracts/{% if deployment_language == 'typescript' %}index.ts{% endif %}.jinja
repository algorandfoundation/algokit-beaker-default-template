import { consoleLogger } from '@algorandfoundation/algokit-utils/types/logging'
import * as algokit from '@algorandfoundation/algokit-utils'
import { deploy as {{ contract_name.split('_')|map('capitalize')|join }}Deployer } from './hello_world/deploy-config'

const contractDeployers = [{{ contract_name.split('_')|map('capitalize')|join }}Deployer]

algokit.Config.configure({
  logger: consoleLogger,
})
;(async () => {
  for (const deployer of contractDeployers) {
    try {
      await deployer()
    } catch (e) {
      console.error(e)
    }
  }
})()
